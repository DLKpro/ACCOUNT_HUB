{
  "$schema": "../../../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capabilities for the AccountHub main window. Phase 0 scaffold: only core window / webview permissions. Additional permissions (dialog:save, shell:allow-open, deep-link, notification) land with the IPC commands that need them per docs/design/05-tauri-ipc.md §Capability model.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
